import { LoadingDirective } from './loading.directive';

describe('LoadingDirective', () => {
  it('should create an instance', () => {
    const directive = new LoadingDirective(null, null, null);
    expect(directive).toBeTruthy();
  });
});
